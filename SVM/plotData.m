% Esta funcao plota os dados em figure ja aberta.
% Seu codigo deve separar as amostras positivas, 
% em que y = 1, das negativas, onde y = 0, em
% cores distintas (nao coloque azul!).
function plotData(X, y)
  
end